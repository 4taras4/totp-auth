# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TOTP' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Base32', '~> 1.1.2'
  pod 'RealmSwift'
  pod 'Fabric'
  pod 'Crashlytics'
  target 'TOTPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TOTPUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
